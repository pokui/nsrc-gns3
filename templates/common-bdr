. common
. address-base

render () {
    DEV="$1"
    CAMPUS="$2"
    HOSTNAME="bdr${DEV}.campus${CAMPUS}"
    . address-mappings
    NREN_V4_HOST="$((4 * ${CAMPUS} - 2))"
    NREN_V4_NET="$((4 * ${CAMPUS} - 4))"
    conf_all
}
